@model DoctorManagement.Models.MedicalExaminations

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>MedicalExaminations</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Examinations)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Examinations)
        
            <table class=" table">
                <tr>
                    <th>ID_Badania</th>
                    <th> ID_Patient</th>
                    <th>Grade</th>
                </tr>
                @foreach (var item in Model.Enrollment)
                {
                    <tr>
                        <td>@Html.DisplayFor(modelItem => item.ID_MedicalExaminations)</td>
                        <td>@Html.DisplayFor(modelItem => item.ID_Patient)</td>
                        <td>@Html.DisplayFor(modelItem => item.Grade)</td>
                    </tr>
                }
            </table>

        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |
    @Html.ActionLink("Back to List", "Index")
</p>
